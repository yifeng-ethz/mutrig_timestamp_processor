# DV Error And Negative Cases — mutrig_timestamp_processor

**Companion docs:** [`DV_PLAN.md`](DV_PLAN.md), [`DV_HARNESS.md`](DV_HARNESS.md), [`DV_COV.md`](DV_COV.md), [`DV_REPORT.md`](DV_REPORT.md)

**Parent:** [DV_PLAN.md](DV_PLAN.md)
**ID Range:** X001-X130
**Total:** 130 cases (130 implemented / 0 waived)

**Methodology key:**
- **D** = Directed
- **R** = Constrained-random

## 1. Summary

| Section | Cases | ID Range | What it Proves | Current Case |
|---------|-------|----------|--------------|-------------|
| Illegal Run-Control Cases | 10 | X001-X010 | Section coverage for Illegal Run-Control Cases | 10/10 |
| CSR Misuse Cases | 10 | X011-X020 | Section coverage for CSR Misuse Cases | 10/10 |
| Input-Error Cases | 10 | X021-X030 | Section coverage for Input-Error Cases | 10/10 |
| Ready / Handshake Misuse | 10 | X031-X040 | Section coverage for Ready / Handshake Misuse | 10/10 |
| Timestamp / Window Fault Cases | 10 | X041-X050 | Section coverage for Timestamp / Window Fault Cases | 10/10 |
| ToT / ET Fault Cases | 10 | X051-X060 | Section coverage for ToT / ET Fault Cases | 10/10 |
| Marker Fault Cases | 10 | X061-X070 | Section coverage for Marker Fault Cases | 10/10 |
| Reset / Recovery Fault Cases | 10 | X071-X080 | Section coverage for Reset / Recovery Fault Cases | 10/10 |
| Generic / Build Fault Cases | 10 | X081-X090 | Section coverage for Generic / Build Fault Cases | 10/10 |
| Debug-Stream Fault Cases | 10 | X091-X100 | Section coverage for Debug-Stream Fault Cases | 10/10 |
| Counter-Coherency Fault Cases | 10 | X101-X110 | Section coverage for Counter-Coherency Fault Cases | 10/10 |
| Termination Failure Injections | 10 | X111-X120 | Section coverage for Termination Failure Injections | 10/10 |
| Upgrade-Contract Regression Gates | 10 | X121-X130 | Section coverage for Upgrade-Contract Regression Gates | 10/10 |

## 2. Illegal Run-Control Cases

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| X001  | D | NEG MTS 001 all zero ctrl word | 1 | Drive control word `000000000`; expect the numeric run-control debug mirror to report `ERROR` and no silent legal-state interpretation. Covers the illegal-zero command. | Drive control word `000000000`; expect the numeric run-control debug mirror to report `ERROR` and no silent legal-state interpretation. Covers the illegal-zero command. | TBD |
| X002  | D | NEG MTS 002 multi hot ctrl word | 1 | Drive a multi-hot control word such as `000001100`; expect the numeric run-control debug mirror to report `ERROR` and no silent legal-state interpretation. Covers the illegal-multi-hot command. | Drive a multi-hot control word such as `000001100`; expect the numeric run-control debug mirror to report `ERROR` and no silent legal-state interpretation. Covers the illegal-multi-hot command. | TBD |
| X003  | D | NEG MTS 003 illegal ctrl during running | 1 | Drive an illegal control word while traffic is flowing in `RUNNING`; verify later legal control commands still work. Covers hostile command injection in the active state. | Drive an illegal control word while traffic is flowing in `RUNNING`; verify later legal control commands still work. Covers hostile command injection in the active state. | TBD |
| X004  | D | NEG MTS 004 illegal ctrl during flushing | 1 | Drive an illegal control word while `FLUSHING` holds control ready low; require it to be ignored until the stop drain completes. Covers hostile command injection during stop drain. | Drive an illegal control word while `FLUSHING` holds control ready low; require it to be ignored until the stop drain completes. Covers hostile command injection during stop drain. | TBD |
| X005  | D | NEG MTS 005 ctrl valid high data changes | 1 | Hold `asi_ctrl_valid=1` while changing `asi_ctrl_data`; require the driver/SVA layer to flag the protocol violation. Covers source misuse. | Hold `asi_ctrl_valid=1` while changing `asi_ctrl_data`; require the driver/SVA layer to flag the protocol violation. Covers source misuse. | TBD |
| X006  | D | NEG MTS 006 ctrl data unknown injection | 1 | Inject X/Z into `asi_ctrl_data` in simulation and require monitor/SVA detection. Covers robustness against undefined control stimulus. | Inject X/Z into `asi_ctrl_data` in simulation and require monitor/SVA detection. Covers robustness against undefined control stimulus. | TBD |
| X007  | D | NEG MTS 007 running without sync documented nonstandard | 1 | Jump directly to `RUNNING` without `RUN_PREPARE/SYNC`; tag it as a supported-but-nonstandard case so later regressions do not misclassify it. Covers a deliberate contract exception. | Jump directly to `RUNNING` without `RUN_PREPARE/SYNC`; tag it as a supported-but-nonstandard case so later regressions do not misclassify it. Covers a deliberate contract exception. | TBD |
| X008  | D | NEG MTS 008 terminate from idle | 1 | Send `TERMINATING` directly from `IDLE`; expect no fake boundary or output activity. Covers a nonsensical stop command. | Send `TERMINATING` directly from `IDLE`; expect no fake boundary or output activity. Covers a nonsensical stop command. | TBD |
| X009  | D | NEG MTS 009 link test during running | 1 | Send `LINK_TEST` during `RUNNING`; verify the unhandled command does not silently corrupt the processor state. Covers an unsupported in-run command. | Send `LINK_TEST` during `RUNNING`; verify the unhandled command does not silently corrupt the processor state. Covers an unsupported in-run command. | TBD |
| X010  | D | NEG MTS 010 always ready masks incomplete work | 1 | Regress the former always-ready control gap; require terminate acknowledgement to wait for close-marker completion. Anchors the upgrade fix. | Regress the former always-ready control gap; require terminate acknowledgement to wait for close-marker completion. Anchors the upgrade fix. | TBD |

## 3. CSR Misuse Cases

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| X011  | D | NEG MTS 011 simultaneous read write same cycle | 1 | Attempt simultaneous CSR read and write in one cycle; require the CSR monitor and scoreboard protocol counters to flag the misuse. Covers AVMM protocol abuse. | Attempt simultaneous CSR read and write in one cycle; require the CSR monitor and scoreboard protocol counters to flag the misuse. Covers AVMM protocol abuse. | TBD |
| X012  | D | NEG MTS 012 write unsupported addr5 | 1 | Write an unsupported CSR address and verify no hidden DUT state changes. Covers out-of-range write misuse. | Write an unsupported CSR address and verify no hidden DUT state changes. Covers out-of-range write misuse. | TBD |
| X013  | D | NEG MTS 013 read unsupported addr6 | 1 | Read an unsupported CSR address and verify the DUT returns zero rather than stale data. Covers out-of-range read misuse. | Read an unsupported CSR address and verify the DUT returns zero rather than stale data. Covers out-of-range read misuse. | TBD |
| X014  | D | NEG MTS 014 reserved opmode bit28 write | 1 | Write only reserved bit 28 and verify no functional state changes. Covers reserved-field misuse. | Write only reserved bit 28 and verify no functional state changes. Covers reserved-field misuse. | TBD |
| X015  | D | NEG MTS 015 write expected latency during reset | 1 | Attempt to write `expected_latency` while global reset is asserted; verify reset dominates. Covers control-plane activity during reset. | Attempt to write `expected_latency` while global reset is asserted; verify reset dominates. Covers control-plane activity during reset. | TBD |
| X016  | D | NEG MTS 016 back to back soft reset pulses | 1 | Pulse `soft_reset` in consecutive cycles and verify the self-clearing logic does not wedge. Covers software abuse of the self-clear bit. | Pulse `soft_reset` in consecutive cycles and verify the self-clearing logic does not wedge. Covers software abuse of the self-clear bit. | TBD |
| X017  | D | NEG MTS 017 rapid force stop toggle | 1 | Toggle `force_stop` on consecutive cycles during traffic and verify the DUT remains deterministic. Covers software thrash on a critical control bit. | Toggle `force_stop` on consecutive cycles during traffic and verify the DUT remains deterministic. Covers software thrash on a critical control bit. | TBD |
| X018  | D | NEG MTS 018 driver ignores waitrequest | 1 | Intentionally build a bad CSR driver that samples read data without respecting `waitrequest`; require the driver analysis port and scoreboard protocol counters to flag the misuse. Covers testbench robustness. | Intentionally build a bad CSR driver that samples read data without respecting `waitrequest`; require the driver analysis port and scoreboard protocol counters to flag the misuse. Covers testbench robustness. | TBD |
| X019  | D | NEG MTS 019 counter reads mid reset | 1 | Read counters during reset transitions and verify the scoreboard treats the transient explicitly rather than silently assuming old values. Covers observability during reset churn. | Read counters during reset transitions and verify the scoreboard treats the transient explicitly rather than silently assuming old values. Covers observability during reset churn. | TBD |
| X020  | D | NEG MTS 020 expected latency overflow model | 1 | Write a huge `expected_latency` that causes `padding_upper` model underflow/wrap effects; require exact reference-model matching. Covers numerically hostile software configuration. | Write a huge `expected_latency` that causes `padding_upper` model underflow/wrap effects; require exact reference-model matching. Covers numerically hostile software configuration. | TBD |

## 4. Input-Error Cases

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| X021  | D | NEG MTS 021 hiterr rejected running | 1 | Drive a hiterr beat in `RUNNING` with discard enabled; expect discard counting and no output beat. Covers the primary error-discard path. | Drive a hiterr beat in `RUNNING` with discard enabled; expect discard counting and no output beat. Covers the primary error-discard path. | TBD |
| X022  | D | NEG MTS 022 hiterr kept running | 1 | Drive the same beat with discard disabled; expect it to propagate. Covers the override path. | Drive the same beat with discard disabled; expect it to propagate. Covers the override path. | TBD |
| X023  | D | NEG MTS 023 crcerr only inert | 1 | Drive only `CRCERR_BIT_LOC=1`; verify the current DUT ignores it. Documents today’s limited integrity checking. | Drive only `CRCERR_BIT_LOC=1`; verify the current DUT ignores it. Documents today’s limited integrity checking. | TBD |
| X024  | D | NEG MTS 024 frame corrupt only inert | 1 | Drive only `FRAME_CORRPT_BIT_LOC=1`; verify the current DUT ignores it. Documents today’s limited integrity checking. | Drive only `FRAME_CORRPT_BIT_LOC=1`; verify the current DUT ignores it. Documents today’s limited integrity checking. | TBD |
| X025  | D | NEG MTS 025 combined error bits only hiterr matters | 1 | Drive all three error bits high and vary `HITERR_BIT_LOC`; verify only the mapped hiterr bit controls acceptance today. Covers combined-error behavior. | Drive all three error bits high and vary `HITERR_BIT_LOC`; verify only the mapped hiterr bit controls acceptance today. Covers combined-error behavior. | TBD |
| X026  | D | NEG MTS 026 valid beat in idle | 1 | Drive valid input while `IDLE`; expect no output and deterministic counting. Covers illegal traffic in quiescent state. | Drive valid input while `IDLE`; expect no output and deterministic counting. Covers illegal traffic in quiescent state. | TBD |
| X027  | D | NEG MTS 027 valid beat in reset sync | 1 | Drive valid input while `RESET/SYNC`; expect no acceptance and deterministic counting. Covers illegal traffic during synchronization. | Drive valid input while `RESET/SYNC`; expect no acceptance and deterministic counting. Covers illegal traffic during synchronization. | TBD |
| X028  | D | NEG MTS 028 valid beat under force stop | 1 | Drive valid input with `force_stop=1`; expect rejection even though ready may remain high. Covers control-induced rejection. | Drive valid input with `force_stop=1`; expect rejection even though ready may remain high. Covers control-induced rejection. | TBD |
| X029  | D | NEG MTS 029 sop without matching eop then abort | 1 | Start a packet and abort before EOP; verify the packet bookkeeping is explicitly tracked and later cleared. Covers malformed packet lifetime. | Start a packet and abort before EOP; verify the packet bookkeeping is explicitly tracked and later cleared. Covers malformed packet lifetime. | TBD |
| X030  | D | NEG MTS 030 sideband outside enabled window | 1 | Drive a sideband channel outside the enabled window while payload remains legal; verify packet bookkeeping does not silently index outside range. Covers malformed sideband use. | Drive a sideband channel outside the enabled window while payload remains legal; verify packet bookkeeping does not silently index outside range. Covers malformed sideband use. | TBD |

## 5. Ready / Handshake Misuse

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| X031  | D | NEG MTS 031 valid while input ready low idle | 1 | Force the source to drive `valid` in `IDLE` despite `ready=0`; require the scoreboard to classify the beat as rejected. Covers source misuse against the handshake. | Force the source to drive `valid` in `IDLE` despite `ready=0`; require the scoreboard to classify the beat as rejected. Covers source misuse against the handshake. | TBD |
| X032  | D | NEG MTS 032 valid while input ready low sync | 1 | Repeat in `RESET/SYNC`; require the scoreboard to classify the beat as rejected. Covers source misuse during synchronization. | Repeat in `RESET/SYNC`; require the scoreboard to classify the beat as rejected. Covers source misuse during synchronization. | TBD |
| X033  | D | NEG MTS 033 source drops valid too early | 1 | Intentionally violate the one-cycle valid contract in the source driver and require bench detection. Covers source protocol misuse. | Intentionally violate the one-cycle valid contract in the source driver and require bench detection. Covers source protocol misuse. | TBD |
| X034  | D | NEG MTS 034 output ready low single fault | 1 | Hold output `ready=0` on a single valid beat and verify the current DUT still emits it; tag the result as a negative protocol finding rather than a scoreboard failure. Documents the ignored-ready source behavior. | Hold output `ready=0` on a single valid beat and verify the current DUT still emits it; tag the result as a negative protocol finding rather than a scoreboard failure. Documents the ignored-ready source behavior. | TBD |
| X035  | D | NEG MTS 035 output ready low boundary fault | 1 | Hold output `ready=0` on the terminating EOP beat and verify the current DUT still emits it; tag it as the same negative protocol finding. Documents the ignored-ready boundary behavior. | Hold output `ready=0` on the terminating EOP beat and verify the current DUT still emits it; tag it as the same negative protocol finding. Documents the ignored-ready boundary behavior. | TBD |
| X036  | D | NEG MTS 036 output ready unknown fault | 1 | Inject X/Z on output `ready` in simulation and require the bench to flag the misuse explicitly. Covers sink-protocol robustness. | Inject X/Z on output `ready` in simulation and require the bench to flag the misuse explicitly. Covers sink-protocol robustness. | TBD |
| X037  | D | NEG MTS 037 csr driver waitrequest fault | 1 | Build a bad CSR driver that changes address/data before `waitrequest` drops; require protocol assertions to fire. Covers bench-side CSR protocol abuse. | Build a bad CSR driver that changes address/data before `waitrequest` drops; require protocol assertions to fire. Covers bench-side CSR protocol abuse. | TBD |
| X038  | D | NEG MTS 038 ctrl driver assumes stateful ready | 1 | Build a run-control driver that waits for `ready` to deassert during prepare/sync/flush and verify the current DUT now does so before restoring `ready`. Documents the stateful-ready handshake required for bring-up. | Build a run-control driver that waits for `ready` to deassert during prepare/sync/flush and verify the current DUT now does so before restoring `ready`. Documents the stateful-ready handshake required for bring-up. | TBD |
| X039  | D | NEG MTS 039 hit source changes payload midbeat | 1 | Intentionally change hit payload fields after asserting `valid`; require the driver/monitor layer to flag the misuse. Covers source data instability. | Intentionally change hit payload fields after asserting `valid`; require the driver/monitor layer to flag the misuse. Covers source data instability. | TBD |
| X040  | D | NEG MTS 040 ctrl valid on reset edge | 1 | Assert `ctrl_valid` exactly on the reset edge and verify the DUT stays in reset-defined state. Covers reset/handshake overlap misuse. | Assert `ctrl_valid` exactly on the reset edge and verify the DUT stays in reset-defined state. Covers reset/handshake overlap misuse. | TBD |

## 6. Timestamp / Window Fault Cases

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| X041  | D | NEG MTS 041 negative debug ts error | 1 | Craft a hit that produces negative `debug_ts`; require `aso_hit_type1_error=1`. Covers the early-hit fault window. | Craft a hit that produces negative `debug_ts`; require `aso_hit_type1_error=1`. Covers the early-hit fault window. | TBD |
| X042  | D | NEG MTS 042 zero debug ts error | 1 | Craft a hit that produces zero `debug_ts`; require `aso_hit_type1_error=1`. Covers the strict-lower-bound fault. | Craft a hit that produces zero `debug_ts`; require `aso_hit_type1_error=1`. Covers the strict-lower-bound fault. | TBD |
| X043  | D | NEG MTS 043 equal expected latency error | 1 | Craft a hit whose debug delay equals the programmed window; require `aso_hit_type1_error=1`. Covers the upper equality fault. | Craft a hit whose debug delay equals the programmed window; require `aso_hit_type1_error=1`. Covers the upper equality fault. | TBD |
| X044  | D | NEG MTS 044 above expected latency error | 1 | Craft a hit above the programmed window; require `aso_hit_type1_error=1`. Covers the upper overflow fault. | Craft a hit above the programmed window; require `aso_hit_type1_error=1`. Covers the upper overflow fault. | TBD |
| X045  | D | NEG MTS 045 zero window fault everything | 1 | Set `expected_latency=0` and verify even minimally delayed hits fault. Covers the degenerate configuration. | Set `expected_latency=0` and verify even minimally delayed hits fault. Covers the degenerate configuration. | TBD |
| X046  | D | NEG MTS 046 bypass toggle midstream mismatch | 1 | Toggle `bypass_lapse` at a hostile time and require exact per-hit reference modeling; any drift is a bug. Covers control/data race sensitivity. | Toggle `bypass_lapse` at a hostile time and require exact per-hit reference modeling; any drift is a bug. Covers control/data race sensitivity. | TBD |
| X047  | D | NEG MTS 047 padding upper regression trap | 1 | Build a case around `padding_upper` where one off-by-one bug would flip correction behavior; require exact match. Covers the most failure-prone padding edge. | Build a case around `padding_upper` where one off-by-one bug would flip correction behavior; require exact match. Covers the most failure-prone padding edge. | TBD |
| X048  | D | NEG MTS 048 quotient remainder mismatch trap | 1 | Build a case where quotient/remainder corruption would be obvious in packed output fields; require exact match. Covers the divide path. | Build a case where quotient/remainder corruption would be obvious in packed output fields; require exact match. Covers the divide path. | TBD |
| X049  | D | NEG MTS 049 route channel mismatch trap | 1 | Require `aso_hit_type1_channel` to match `tcc_8n[5:4]` exactly on every beat; any divergence is a failure. Covers the route-sideband contract. | Require `aso_hit_type1_channel` to match `tcc_8n[5:4]` exactly on every beat; any divergence is a failure. Covers the route-sideband contract. | TBD |
| X050  | D | NEG MTS 050 tfine corruption trap | 1 | Require `TFine` to pass through unchanged under hostile traffic; any divergence is a failure. Covers a simple but critical payload field. | Require `TFine` to pass through unchanged under hostile traffic; any divergence is a failure. Covers a simple but critical payload field. | TBD |

## 7. ToT / ET Fault Cases

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| X051  | D | NEG MTS 051 short mode nonzero et illegal | 1 | In short mode, any nonzero ET is a failure. Covers accidental long-mode leakage. | In short mode, any nonzero ET is a failure. Covers accidental long-mode leakage. | TBD |
| X052  | D | NEG MTS 052 tot mode eflag0 nonzero et illegal | 1 | In ToT mode with `EFlag=0`, any nonzero ET is a failure. Covers missed mask behavior. | In ToT mode with `EFlag=0`, any nonzero ET is a failure. Covers missed mask behavior. | TBD |
| X053  | D | NEG MTS 053 positive delta missing et | 1 | For a positive decoded delta, zero ET is a failure unless the reference model predicts clamp/mask. Covers lost-ToT bugs. | For a positive decoded delta, zero ET is a failure unless the reference model predicts clamp/mask. Covers lost-ToT bugs. | TBD |
| X054  | D | NEG MTS 054 above 511 unsaturated et | 1 | For a delta above saturation, any ET other than 511 is a failure. Covers lost saturation. | For a delta above saturation, any ET other than 511 is a failure. Covers lost saturation. | TBD |
| X055  | D | NEG MTS 055 negative delta wrong clamp | 1 | For the negative-delta reference vector, any behavior that deviates from the current agreed reference is a failure. Covers the area that changed recently. | For the negative-delta reference vector, any behavior that deviates from the current agreed reference is a failure. Covers the area that changed recently. | TBD |
| X056  | D | NEG MTS 056 stale derive tot after toggle | 1 | Toggle `derive_tot` and require each accepted hit to use the correct sampled mode; stale mode usage is a failure. Covers control-to-data drift. | Toggle `derive_tot` and require each accepted hit to use the correct sampled mode; stale mode usage is a failure. Covers control-to-data drift. | TBD |
| X057  | D | NEG MTS 057 stale delay field after toggle | 1 | Toggle the delay-field selector and require debug/error logic to follow immediately for newly accepted hits; stale usage is a failure. Covers control-to-debug drift. | Toggle the delay-field selector and require debug/error logic to follow immediately for newly accepted hits; stale usage is a failure. Covers control-to-debug drift. | TBD |
| X058  | D | NEG MTS 058 eflag pipeline corruption | 1 | Alternate `EFlag` values under load and require ET behavior to follow each hit’s flag exactly. Covers flag-data coupling. | Alternate `EFlag` values under load and require ET behavior to follow each hit’s flag exactly. Covers flag-data coupling. | TBD |
| X059  | D | NEG MTS 059 legacy positive vector regression | 1 | Re-run the checked-in positive smoke vector and treat any mismatch as a failure. Keeps the current TB contract binding. | Re-run the checked-in positive smoke vector and treat any mismatch as a failure. Keeps the current TB contract binding. | TBD |
| X060  | D | NEG MTS 060 legacy clamp vector regression | 1 | Re-run the checked-in clamp/saturation smoke vector and treat any mismatch as a failure. Keeps the current TB contract binding. | Re-run the checked-in clamp/saturation smoke vector and treat any mismatch as a failure. Keeps the current TB contract binding. | TBD |

## 8. Marker Fault Cases

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| X061  | D | NEG MTS 061 missing first sop | 1 | Require SOP on the first enabled-channel hit of a run; absence is a failure. Covers marker omission. | Require SOP on the first enabled-channel hit of a run; absence is a failure. Covers marker omission. | TBD |
| X062  | D | NEG MTS 062 repeated sop same channel | 1 | Require no repeated SOP for the same enabled channel in one run; repetition is a failure. Covers marker duplication. | Require no repeated SOP for the same enabled channel in one run; repetition is a failure. Covers marker duplication. | TBD |
| X063  | D | NEG MTS 063 sop on disabled channel | 1 | Drive an input sideband outside the enabled window and verify route-lane SOP still follows the output route while disabled input-side packet bookkeeping does not hold termination open. Covers the split between route markers and input window tracking. | Drive an input sideband outside the enabled window and verify route-lane SOP still follows the output route while disabled input-side packet bookkeeping does not hold termination open. Covers the split between route markers and input window tracking. | TBD |
| X064  | D | NEG MTS 064 eop outside terminating illegal | 1 | Require no output EOP outside termination scenarios; any assertion is a failure. Covers boundary leakage. | Require no output EOP outside termination scenarios; any assertion is a failure. Covers boundary leakage. | TBD |
| X065  | D | NEG MTS 065 missing forwarded terminating eop | 1 | Accept a terminating input EOP and require the terminal empty close-marker train; absence is a failure. Covers the current stop contract. | Accept a terminating input EOP and require the terminal empty close-marker train; absence is a failure. Covers the current stop contract. | TBD |
| X066  | D | NEG MTS 066 eop pipe alignment hole | 1 | Create the case where a delayed EOP pulse can miss `hit_out.valid`; if the boundary is lost, record it as a current design problem. Covers a known alignment weakness. | Create the case where a delayed EOP pulse can miss `hit_out.valid`; if the boundary is lost, record it as a current design problem. Covers a known alignment weakness. | TBD |
| X067  | D | NEG MTS 067 empty nonzero illegal | 1 | Require payload beats to keep `aso_hit_type1_empty=0`; terminal close markers are checked separately. Covers accidental empty assertion on payload traffic. | Require payload beats to keep `aso_hit_type1_empty=0`; terminal close markers are checked separately. Covers accidental empty assertion on payload traffic. | TBD |
| X068  | D | NEG MTS 068 duplicate output eop | 1 | Accept one terminating input EOP and require exactly one terminal close-marker train across the route lanes; duplicate markers are a failure. Covers boundary duplication. | Accept one terminating input EOP and require exactly one terminal close-marker train across the route lanes; duplicate markers are a failure. Covers boundary duplication. | TBD |
| X069  | D | NEG MTS 069 output valid outside active states | 1 | Require no output-valid beats outside `RUNNING` or `FLUSHING`; any beat is a failure. Covers state-gating regressions. | Require no output-valid beats outside `RUNNING` or `FLUSHING`; any beat is a failure. Covers state-gating regressions. | TBD |
| X070  | D | NEG MTS 070 packet tracker not cleared by reset | 1 | Require reset to clear packet bookkeeping; any stale in-transaction state is a failure. Covers packet-state cleanup. | Require reset to clear packet bookkeeping; any stale in-transaction state is a failure. Covers packet-state cleanup. | TBD |

## 9. Reset / Recovery Fault Cases

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| X071  | D | NEG MTS 071 global reset clears inflight valids | 1 | Assert global reset with data in flight and require all stage valids to clear. Covers hard reset cleanup. | Assert global reset with data in flight and require all stage valids to clear. Covers hard reset cleanup. | TBD |
| X072  | D | NEG MTS 072 global reset clears debug history | 1 | Assert global reset with debug history active and require subsequent debug outputs to restart from zero. Covers hard reset cleanup of side streams. | Assert global reset with debug history active and require subsequent debug outputs to restart from zero. Covers hard reset cleanup of side streams. | TBD |
| X073  | D | NEG MTS 073 soft reset hangs running illegal | 1 | Pulse `soft_reset` in `RUNNING` and require the DUT to continue operating afterward; any hang is a failure. Covers software reset recovery. | Pulse `soft_reset` in `RUNNING` and require the DUT to continue operating afterward; any hang is a failure. Covers software reset recovery. | TBD |
| X074  | D | NEG MTS 074 soft reset creates phantom eop | 1 | Pulse `soft_reset` in `FLUSHING` and require no spurious output EOP. Covers reset/marker interaction. | Pulse `soft_reset` in `FLUSHING` and require no spurious output EOP. Covers reset/marker interaction. | TBD |
| X075  | D | NEG MTS 075 prepare after aborted packet | 1 | Abort a packet with `IDLE`, then re-enter the standard start sequence; any stale SOP/EOP bookkeeping is a failure. Covers recovery from malformed stops. | Abort a packet with `IDLE`, then re-enter the standard start sequence; any stale SOP/EOP bookkeeping is a failure. Covers recovery from malformed stops. | TBD |
| X076  | D | NEG MTS 076 force stop stuck high | 1 | Leave `force_stop` high for a long time and require no emitted payloads; attempts may be counted and discarded, but any output beat is a failure. Covers control override stability. | Leave `force_stop` high for a long time and require no emitted payloads; attempts may be counted and discarded, but any output beat is a failure. Covers control override stability. | TBD |
| X077  | D | NEG MTS 077 force stop clear not reopening | 1 | Clear `force_stop` and require acceptance to resume immediately in `RUNNING`; failure to reopen is a bug. Covers override recovery. | Clear `force_stop` and require acceptance to resume immediately in `RUNNING`; failure to reopen is a bug. Covers override recovery. | TBD |
| X078  | D | NEG MTS 078 reset flow stuck sclr | 1 | After `RUN_PREPARE -> SYNC -> RUNNING`, require `reset_flow` not to remain stuck in `SCLR`; any such stickiness is a failure. Covers state-machine advancement. | After `RUN_PREPARE -> SYNC -> RUNNING`, require `reset_flow` not to remain stuck in `SCLR`; any such stickiness is a failure. Covers state-machine advancement. | TBD |
| X079  | D | NEG MTS 079 reset flow stuck sync | 1 | After `RUNNING`, require `reset_flow` not to remain stuck in `SYNC`; any such stickiness is a failure. Covers state-machine advancement. | After `RUNNING`, require `reset_flow` not to remain stuck in `SYNC`; any such stickiness is a failure. Covers state-machine advancement. | TBD |
| X080  | D | NEG MTS 080 direct running no accept illegal | 1 | Use the legacy direct start and require clean acceptance; failure is a regression against the current contract. Covers backward-compatibility. | Use the legacy direct start and require clean acceptance; failure is a regression against the current contract. Covers backward-compatibility. | TBD |

## 10. Generic / Build Fault Cases

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| X081  | D | NEG MTS 081 pipeline two math regression | 1 | Compare `LPM_DIV_PIPELINE=2` against `4` and require identical math results aside from latency; any arithmetic difference is a failure. Covers packaged-build correctness. | Compare `LPM_DIV_PIPELINE=2` against `4` and require identical math results aside from latency; any arithmetic difference is a failure. Covers packaged-build correctness. | TBD |
| X082  | D | NEG MTS 082 remapped hiterr not honored | 1 | Remap `HITERR_BIT_LOC` and require the new bit to control discard; failure indicates the generic is not applied. Covers generic correctness. | Remap `HITERR_BIT_LOC` and require the new bit to control discard; failure indicates the generic is not applied. Covers generic correctness. | TBD |
| X083  | D | NEG MTS 083 default latency generic not reflected | 1 | Change `MUTRIG_BUFFER_EXPECTED_LATENCY_8N` at build time and require power-on CSR readback to follow it; failure indicates generic-to-CSR breakage. Covers generic correctness. | Change `MUTRIG_BUFFER_EXPECTED_LATENCY_8N` at build time and require power-on CSR readback to follow it; failure indicates generic-to-CSR breakage. Covers generic correctness. | TBD |
| X084  | D | NEG MTS 084 debug zero changes functionality | 1 | Compare `DEBUG=0` and `DEBUG=1`; any functional difference is a failure. Covers debug-gating correctness. | Compare `DEBUG=0` and `DEBUG=1`; any functional difference is a failure. Covers debug-gating correctness. | TBD |
| X085  | D | NEG MTS 085 bank string changes functionality | 1 | Compare `BANK` builds; any functional difference is a failure. Covers a nominally cosmetic generic. | Compare `BANK` builds; any functional difference is a failure. Covers a nominally cosmetic generic. | TBD |
| X086  | D | NEG MTS 086 padding eop wait changes behavior today | 1 | Sweep `PADDING_EOP_WAIT_CYCLE`; any current functional change is a failure because the generic is not wired. Covers inert-generic regression. | Sweep `PADDING_EOP_WAIT_CYCLE`; any current functional change is a failure because the generic is not wired. Covers inert-generic regression. | TBD |
| X087  | D | NEG MTS 087 crcerr bit changes behavior today | 1 | Sweep `CRCERR_BIT_LOC`; any current functional change is a failure because the bit is not consumed. Covers inert-generic regression. | Sweep `CRCERR_BIT_LOC`; any current functional change is a failure because the bit is not consumed. Covers inert-generic regression. | TBD |
| X088  | D | NEG MTS 088 frame corrupt bit changes behavior today | 1 | Sweep `FRAME_CORRPT_BIT_LOC`; any current functional change is a failure because the bit is not consumed. Covers inert-generic regression. | Sweep `FRAME_CORRPT_BIT_LOC`; any current functional change is a failure because the bit is not consumed. Covers inert-generic regression. | TBD |
| X089  | D | NEG MTS 089 invalid enabled window compile guard | 1 | Use an invalid enabled-channel window and require the build or configuration layer to reject it explicitly. Covers configuration sanity. | Use an invalid enabled-channel window and require the build or configuration layer to reject it explicitly. Covers configuration sanity. | TBD |
| X090  | D | NEG MTS 090 out of range enabled values | 1 | Try out-of-range enabled-channel values and require compile-time or elaboration-time rejection. Covers configuration sanity. | Try out-of-range enabled-channel values and require compile-time or elaboration-time rejection. Covers configuration sanity. | TBD |

## 11. Debug-Stream Fault Cases

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| X091  | D | NEG MTS 091 debug ts without processed hit | 1 | Require `debug_ts_valid` only when a processed hit is present; any free-running debug-ts beat is a failure. Covers side-stream ghost traffic. | Require `debug_ts_valid` only when a processed hit is present; any free-running debug-ts beat is a failure. Covers side-stream ghost traffic. | TBD |
| X092  | D | NEG MTS 092 stale debug ts data | 1 | Require `debug_ts_data` to update coherently when `debug_ts_valid` toggles; stale data is a failure. Covers side-stream data freshness. | Require `debug_ts_data` to update coherently when `debug_ts_valid` toggles; stale data is a failure. Covers side-stream data freshness. | TBD |
| X093  | D | NEG MTS 093 debug burst on first hit without history | 1 | Require the first hit after entering `RUNNING` not to produce a bogus stale-delta burst. Covers history warm-up faults. | Require the first hit after entering `RUNNING` not to produce a bogus stale-delta burst. Covers history warm-up faults. | TBD |
| X094  | D | NEG MTS 094 ts delta without burst context | 1 | Require `ts_delta_valid` to remain aligned to the same second-hit history conditions as `debug_burst_valid`; any mismatch is a failure. Covers debug-stream coherence. | Require `ts_delta_valid` to remain aligned to the same second-hit history conditions as `debug_burst_valid`; any mismatch is a failure. Covers debug-stream coherence. | TBD |
| X095  | D | NEG MTS 095 debug burst and ts delta sign disagree | 1 | For the same hit pair, require both debug outputs to agree on sign; disagreement is a failure. Covers sign-conversion coherence. | For the same hit pair, require both debug outputs to agree on sign; disagreement is a failure. Covers sign-conversion coherence. | TBD |
| X096  | D | NEG MTS 096 arrival delta wrap fault | 1 | Drive a hostile arrival-timing pattern and require the scoreboard to detect any underflow-wrap bug in the arrival delta. Covers debug lower-byte correctness. | Drive a hostile arrival-timing pattern and require the scoreboard to detect any underflow-wrap bug in the arrival delta. Covers debug lower-byte correctness. | TBD |
| X097  | D | NEG MTS 097 signmag conversion extreme negative | 1 | Craft the most negative representable sign-magnitude delta and require `ts_delta` conversion to match the helper function exactly. Covers conversion extremes. | Craft the most negative representable sign-magnitude delta and require `ts_delta` conversion to match the helper function exactly. Covers conversion extremes. | TBD |
| X098  | D | NEG MTS 098 delay field switch no debug source change | 1 | Toggle the delay-field selector and require a corresponding change in debug-ts source; failure means stale path selection. Covers control/debug coupling. | Toggle the delay-field selector and require a corresponding change in debug-ts source; failure means stale path selection. Covers control/debug coupling. | TBD |
| X099  | D | NEG MTS 099 debug outputs active in idle | 1 | Require all debug-valid outputs low in `IDLE`; any activity is a failure. Covers quiescent-state cleanliness. | Require all debug-valid outputs low in `IDLE`; any activity is a failure. Covers quiescent-state cleanliness. | TBD |
| X100  | D | NEG MTS 100 debug outputs active in reset | 1 | Require all debug-valid outputs low during global reset; any activity is a failure. Covers reset-state cleanliness. | Require all debug-valid outputs low during global reset; any activity is a failure. Covers reset-state cleanliness. | TBD |

## 12. Counter-Coherency Fault Cases

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| X101  | D | NEG MTS 101 discard counter on clean hit | 1 | Require the discard counter not to increment on an accepted clean hit; any increment is a failure. Covers false discard counting. | Require the discard counter not to increment on an accepted clean hit; any increment is a failure. Covers false discard counting. | TBD |
| X102  | D | NEG MTS 102 missing discard increment | 1 | Require the discard counter to increment on a rejected hiterr beat; absence is a failure. Covers missed discard counting. | Require the discard counter to increment on a rejected hiterr beat; absence is a failure. Covers missed discard counting. | TBD |
| X103  | D | NEG MTS 103 missing total increment on reject | 1 | Require the total counter to increment even on rejected beats; absence is a failure. Covers ingress-accounting regressions. | Require the total counter to increment even on rejected beats; absence is a failure. Covers ingress-accounting regressions. | TBD |
| X104  | D | NEG MTS 104 spurious total increment without valid | 1 | Require the total counter not to increment without `asi_hit_type0_valid`; any increment is a failure. Covers false ingress accounting. | Require the total counter not to increment without `asi_hit_type0_valid`; any increment is a failure. Covers false ingress accounting. | TBD |
| X105  | D | NEG MTS 105 hi lo counter snapshot incoherent | 1 | Poll high/low counters around rollover and treat incoherent snapshots as a failure unless the software read method accounts for them. Covers software-visible counting correctness. | Poll high/low counters around rollover and treat incoherent snapshots as a failure unless the software read method accounts for them. Covers software-visible counting correctness. | TBD |
| X106  | D | NEG MTS 106 soft reset counter clear failure | 1 | Pulse `soft_reset` and require both counters to clear; any stale count is a failure. Covers software-reset accounting. | Pulse `soft_reset` and require both counters to clear; any stale count is a failure. Covers software-reset accounting. | TBD |
| X107  | D | NEG MTS 107 sync counter clear failure | 1 | Execute the standard prepare/sync sequence and require both counters to clear; any stale count is a failure. Covers run-sequence accounting. | Execute the standard prepare/sync sequence and require both counters to clear; any stale count is a failure. Covers run-sequence accounting. | TBD |
| X108  | D | NEG MTS 108 running status high outside run | 1 | Require CSR bit 0 low outside `RUNNING`; any high value is a failure. Covers control/status truthfulness. | Require CSR bit 0 low outside `RUNNING`; any high value is a failure. Covers control/status truthfulness. | TBD |
| X109  | D | NEG MTS 109 running status low inside run | 1 | Require CSR bit 0 high in `RUNNING`; any low value is a failure. Covers control/status truthfulness. | Require CSR bit 0 high in `RUNNING`; any low value is a failure. Covers control/status truthfulness. | TBD |
| X110  | D | NEG MTS 110 control readback mismatch | 1 | Require `force_stop`, `soft_reset`, `bypass_lapse`, `discard_hiterr`, and op-mode fields to read back as implemented; mismatch is a failure. Covers software-visible control state. | Require `force_stop`, `soft_reset`, `bypass_lapse`, `discard_hiterr`, and op-mode fields to read back as implemented; mismatch is a failure. Covers software-visible control state. | TBD |

## 13. Termination Failure Injections

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| X111  | D | NEG MTS 111 terminate without real eop gap | 1 | Enter `TERMINATING`, provide upstream end-of-run without a real input EOP, and require the deterministic four-lane terminal close-marker train. Covers the synthetic stop boundary path. | Enter `TERMINATING`, provide upstream end-of-run without a real input EOP, and require the deterministic four-lane terminal close-marker train. Covers the synthetic stop boundary path. | TBD |
| X112  | D | NEG MTS 112 idle before eop delay finishes | 1 | Enter `TERMINATING`, accept EOP, then issue `IDLE` before the delayed pulse matures; require the boundary and ready recovery to remain ordered. Covers control precedence over stop bookkeeping. | Enter `TERMINATING`, accept EOP, then issue `IDLE` before the delayed pulse matures; require the boundary and ready recovery to remain ordered. Covers control precedence over stop bookkeeping. | TBD |
| X113  | D | NEG MTS 113 multiple eops multiple boundaries | 1 | Feed multiple EOP-tagged beats in `FLUSHING` and require one terminal boundary train, not unbounded duplicate boundaries. Covers over-generation of stop markers. | Feed multiple EOP-tagged beats in `FLUSHING` and require one terminal boundary train, not unbounded duplicate boundaries. Covers over-generation of stop markers. | TBD |
| X114  | D | NEG MTS 114 packet crosses terminate edge | 1 | Start a packet before `TERMINATING` and close it after the stop edge; require exactly one boundary and correct payload retention. Covers the core stop-crossing scenario. | Start a packet before `TERMINATING` and close it after the stop edge; require exactly one boundary and correct payload retention. Covers the core stop-crossing scenario. | TBD |
| X115  | D | NEG MTS 115 disabled sideband boundary loss | 1 | Use a sideband channel outside the enabled input window during a terminating EOP and still require deterministic route-lane payload/debug evidence plus terminal close markers. Covers sideband/window stop mismatch. | Use a sideband channel outside the enabled input window during a terminating EOP and still require deterministic route-lane payload/debug evidence plus terminal close markers. Covers sideband/window stop mismatch. | TBD |
| X116  | D | NEG MTS 116 terminate ack before work done | 1 | Observe `asi_ctrl_ready` during termination, require acknowledgement to reflect drain and close-marker completion, then accept `RUNNING` only after that completion point. Covers the stateful completion handshake. | Observe `asi_ctrl_ready` during termination, require acknowledgement to reflect drain and close-marker completion, then accept `RUNNING` only after that completion point. Covers the stateful completion handshake. | TBD |
| X117  | D | NEG MTS 117 flushing accepts fresh hits upgrade gap | 1 | Continue driving fresh hits in `FLUSHING` and record the delivered behavior: the current RTL accepts them, pairs normal/debug metadata, and classifies clean versus delay-error payloads explicitly. Covers post-stop openness. | Continue driving fresh hits in `FLUSHING` and record the delivered behavior: the current RTL accepts them, pairs normal/debug metadata, and classifies clean versus delay-error payloads explicitly. Covers post-stop openness. | TBD |
| X118  | D | NEG MTS 118 missing boundary with packet open | 1 | Track a packet-in-transaction case and require a downstream terminal boundary after upstream end-of-run; this is the BUG-015-R regression. Covers packet-aware stop correctness. | Track a packet-in-transaction case and require a downstream terminal boundary after upstream end-of-run; this is the BUG-015-R regression. Covers packet-aware stop correctness. | TBD |
| X119  | D | NEG MTS 119 duplicate boundary per run | 1 | Require exactly one terminal boundary train per run stop, even when late payloads and multiple EOP attempts are present. Covers end-of-run uniqueness. | Require exactly one terminal boundary train per run stop, even when late payloads and multiple EOP attempts are present. Covers end-of-run uniqueness. | TBD |
| X120  | D | NEG MTS 120 idle before pipeline empty | 1 | Force `IDLE` while in-flight data remains and require no silent data loss; any loss is a failure. Covers premature-stop cleanup. | Force `IDLE` while in-flight data remains and require no silent data loss; any loss is a failure. Covers premature-stop cleanup. | TBD |

## 14. Upgrade-Contract Regression Gates

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| X121  | D | NEG MTS 121 prepare ready stateful upgrade | 1 | Require `asi_ctrl_ready` to deassert and restore deterministically around `RUN_PREPARE`. Keeps the start-sequence ready contract visible for hardware bring-up. | Require `asi_ctrl_ready` to deassert and restore deterministically around `RUN_PREPARE`. Keeps the start-sequence ready contract visible for hardware bring-up. | TBD |
| X122  | D | NEG MTS 122 sync ready stateful upgrade | 1 | Require `asi_ctrl_ready` to deassert and restore deterministically around `SYNC`. Keeps synchronization ready behavior explicit. | Require `asi_ctrl_ready` to deassert and restore deterministically around `SYNC`. Keeps synchronization ready behavior explicit. | TBD |
| X123  | D | NEG MTS 123 flushing ready stateful upgrade | 1 | Require `asi_ctrl_ready` to deassert during `FLUSHING` until the terminal close-marker work is complete. Covers stop-sequence completion visibility. | Require `asi_ctrl_ready` to deassert during `FLUSHING` until the terminal close-marker work is complete. Covers stop-sequence completion visibility. | TBD |
| X124  | D | NEG MTS 124 terminate ack after drain upgrade | 1 | Require terminate acknowledgement to wait for payload drain and terminal boundary completion. Covers completion-based control handshaking. | Require terminate acknowledgement to wait for payload drain and terminal boundary completion. Covers completion-based control handshaking. | TBD |
| X125  | D | NEG MTS 125 synthetic boundary without real eop upgrade | 1 | Require a deterministic terminal boundary after upstream end-of-run even without a real input EOP. Covers synthetic stop-boundary generation. | Require a deterministic terminal boundary after upstream end-of-run even without a real input EOP. Covers synthetic stop-boundary generation. | TBD |
| X126  | D | NEG MTS 126 no fresh accept in flushing upgrade | 1 | Retain the future no-fresh-accept name but record current delivered RTL behavior: fresh FLUSHING hits are accepted and checked with normal/debug trace classification. This remains the upgrade-gap checkpoint for a later policy change. | Retain the future no-fresh-accept name but record current delivered RTL behavior: fresh FLUSHING hits are accepted and checked with normal/debug trace classification. This remains the upgrade-gap checkpoint for a later policy change. | TBD |
| X127  | D | NEG MTS 127 exactly one boundary per stop upgrade | 1 | Require each stop to produce exactly one terminal boundary train under late-EOP stress. Covers terminal-boundary uniqueness. | Require each stop to produce exactly one terminal boundary train under late-EOP stress. Covers terminal-boundary uniqueness. | TBD |
| X128  | D | NEG MTS 128 idle only after boundary upgrade | 1 | Require `IDLE` sequencing only after boundary forwarding has completed. Covers final stop-to-idle ordering. | Require `IDLE` sequencing only after boundary forwarding has completed. Covers final stop-to-idle ordering. | TBD |
| X129  | D | NEG MTS 129 ctrl handshake reflects completion upgrade | 1 | Require the control handshake to reflect completed work, not just command sampling, and prove a restart command is accepted only after terminal work is complete. Covers bring-up-visible completion semantics. | Require the control handshake to reflect completed work, not just command sampling, and prove a restart command is accepted only after terminal work is complete. Covers bring-up-visible completion semantics. | TBD |
| X130  | D | NEG MTS 130 full run sequence upgrade suite | 1 | Bundle the termination, ready, synthetic-boundary, exact-boundary, and mixed-soak contracts into the full upgrade-regression suite. Creates the final run-sequence signoff gate. | Bundle the termination, ready, synthetic-boundary, exact-boundary, and mixed-soak contracts into the full upgrade-regression suite. Creates the final run-sequence signoff gate. | TBD |
