# DV Basic Cases — mutrig_timestamp_processor

**Companion docs:** [`DV_PLAN.md`](DV_PLAN.md), [`DV_HARNESS.md`](DV_HARNESS.md), [`DV_COV.md`](DV_COV.md), [`DV_REPORT.md`](DV_REPORT.md)

**Parent:** [DV_PLAN.md](DV_PLAN.md)
**ID Range:** B001-B130
**Total:** 130 cases (130 implemented / 0 waived)

**Methodology key:**
- **D** = Directed
- **R** = Constrained-random

## 1. Summary

| Section | Cases | ID Range | What it Proves | Current Case |
|---------|-------|----------|--------------|-------------|
| Reset And Bring-Up | 10 | B001-B010 | Section coverage for Reset And Bring-Up | 10/10 |
| CSR Defaults And Control Bits | 10 | B011-B020 | Section coverage for CSR Defaults And Control Bits | 10/10 |
| CSR Semantics And Counter Behavior | 10 | B021-B030 | Section coverage for CSR Semantics And Counter Behavior | 10/10 |
| Input Acceptance And Discard Paths | 10 | B031-B040 | Section coverage for Input Acceptance And Discard Paths | 10/10 |
| Run-Control Sequence Baselines | 10 | B041-B050 | Section coverage for Run-Control Sequence Baselines | 10/10 |
| Timestamp Decode And Padding Basics | 10 | B051-B060 | Section coverage for Timestamp Decode And Padding Basics | 10/10 |
| ET / Op-Mode Basics | 10 | B061-B070 | Section coverage for ET / Op-Mode Basics | 10/10 |
| Output Marker Basics | 10 | B071-B080 | Section coverage for Output Marker Basics | 10/10 |
| Output Routing And Error Basics | 10 | B081-B090 | Section coverage for Output Routing And Error Basics | 10/10 |
| Debug-Burst And Delta Basics | 10 | B091-B100 | Section coverage for Debug-Burst And Delta Basics | 10/10 |
| Legacy Smoke And Counter Checks | 10 | B101-B110 | Section coverage for Legacy Smoke And Counter Checks | 10/10 |
| Parameter And Generic Baselines | 10 | B111-B120 | Section coverage for Parameter And Generic Baselines | 10/10 |
| Termination And Upgrade-Gating Basics | 10 | B121-B130 | Section coverage for Termination And Upgrade-Gating Basics | 10/10 |

## 2. Reset And Bring-Up

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| B001  | D | STD MTS 001 powerup reset idle | 1 | Hold `i_rst=1` with all interfaces quiet; expect `run_state_cmd=IDLE`, `processor_state=IDLE`, `asi_hit_type0_ready=0`, all output valids low, and counters cleared. Proves safe power-up. | Hold `i_rst=1` with all interfaces quiet; expect `run_state_cmd=IDLE`, `processor_state=IDLE`, `asi_hit_type0_ready=0`, all output valids low, and counters cleared. Proves safe power-up. | TBD |
| B002  | D | STD MTS 002 reset release idle quiet | 1 | Release `i_rst` without sending control words; expect the DUT to remain quiescent in `IDLE` and not emit stale `hit_type1`, `debug_ts`, `debug_burst`, or `ts_delta`. Catches reset-release garbage. | Release `i_rst` without sending control words; expect the DUT to remain quiescent in `IDLE` and not emit stale `hit_type1`, `debug_ts`, `debug_burst`, or `ts_delta`. Catches reset-release garbage. | TBD |
| B003  | D | STD MTS 003 direct running entry allowed | 1 | Send only the `RUNNING` control word after reset; expect the current RTL to enter `RUNNING` directly because `csr.go` defaults high. Locks down the legacy nonstandard fast-entry path. | Send only the `RUNNING` control word after reset; expect the current RTL to enter `RUNNING` directly because `csr.go` defaults high. Locks down the legacy nonstandard fast-entry path. | TBD |
| B004  | D | STD MTS 004 run prepare enters reset sclr | 1 | From `IDLE`, send `RUN_PREPARE`; expect `processor_state=RESET`, `reset_flow=SCLR`, and `asi_hit_type0_ready=1` for flush acceptance. Verifies the standard first step. | From `IDLE`, send `RUN_PREPARE`; expect `processor_state=RESET`, `reset_flow=SCLR`, and `asi_hit_type0_ready=1` for flush acceptance. Verifies the standard first step. | TBD |
| B005  | D | STD MTS 005 sync enters reset sync | 1 | Follow `RUN_PREPARE` with `SYNC`; expect `reset_flow=SYNC`, input ready low, and the local counters held/reset. Verifies the second step of the standard sequence. | Follow `RUN_PREPARE` with `SYNC`; expect `reset_flow=SYNC`, input ready low, and the local counters held/reset. Verifies the second step of the standard sequence. | TBD |
| B006  | D | STD MTS 006 running from sync | 1 | After `SYNC`, send `RUNNING`; expect `processor_state=RUNNING`, acceptance of fresh hits, and counter advancement. Confirms the legal arm-to-run transition. | After `SYNC`, send `RUNNING`; expect `processor_state=RUNNING`, acceptance of fresh hits, and counter advancement. Confirms the legal arm-to-run transition. | TBD |
| B007  | D | STD MTS 007 terminating enters flushing | 1 | From `RUNNING`, send `TERMINATING`; expect `processor_state=FLUSHING` and continued input readiness under the current contract. Proves the explicit stop-state entry. | From `RUNNING`, send `TERMINATING`; expect `processor_state=FLUSHING` and continued input readiness under the current contract. Proves the explicit stop-state entry. | TBD |
| B008  | D | STD MTS 008 idle from flushing | 1 | From `FLUSHING`, assert upstream `endofrun`, wait for the close-marker train, then send `IDLE`; expect quiescent outputs and no lingering debug activity after the return. Verifies orderly return to idle under the stateful control-ready contract. | From `FLUSHING`, assert upstream `endofrun`, wait for the close-marker train, then send `IDLE`; expect quiescent outputs and no lingering debug activity after the return. Verifies orderly return to idle under the stateful control-ready contract. | TBD |
| B009  | D | STD MTS 009 running abort to idle | 1 | From `RUNNING`, send `IDLE` directly; expect output silence and no further hit acceptance. Covers the direct abort path implemented in the FSM. | From `RUNNING`, send `IDLE` directly; expect output silence and no further hit acceptance. Covers the direct abort path implemented in the FSM. | TBD |
| B010  | D | STD MTS 010 global reset during flushing | 1 | Assert `i_rst` while the DUT is in `FLUSHING`; expect all pipeline valids, marker pipes, and debug histories to clear immediately. Catches stuck in-flight state. | Assert `i_rst` while the DUT is in `FLUSHING`; expect all pipeline valids, marker pipes, and debug histories to clear immediately. Catches stuck in-flight state. | TBD |

## 3. CSR Defaults And Control Bits

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| B011  | D | STD MTS 011 control readback after reset | 1 | Read CSR word `0x0` after reset; expect bit 0 to read `0` because it reports live RUNNING state even though internal `csr.go` reset value is `1`. Locks down the status-vs-storage quirk. | Read CSR word `0x0` after reset; expect bit 0 to read `0` because it reports live RUNNING state even though internal `csr.go` reset value is `1`. Locks down the status-vs-storage quirk. | TBD |
| B012  | D | STD MTS 012 discard counter default zero | 1 | Read CSR word `0x4` after reset; expect the discarded-hit counter to be zero. Verifies clean debug state. | Read CSR word `0x4` after reset; expect the discarded-hit counter to be zero. Verifies clean debug state. | TBD |
| B013  | D | STD MTS 013 expected latency default 2000 | 1 | Read CSR word `0x8` after reset; expect `2000` per the generic default. Confirms the software-visible power-on window. | Read CSR word `0x8` after reset; expect `2000` per the generic default. Confirms the software-visible power-on window. | TBD |
| B014  | D | STD MTS 014 total counter hi default zero | 1 | Read CSR word `0xC` after reset; expect the upper hit-count word to be zero. Guards reset-map correctness. | Read CSR word `0xC` after reset; expect the upper hit-count word to be zero. Guards reset-map correctness. | TBD |
| B015  | D | STD MTS 015 total counter lo default zero | 1 | Read CSR word `0x10` after reset; expect the lower hit-count word to be zero. Guards reset-map correctness. | Read CSR word `0x10` after reset; expect the lower hit-count word to be zero. Guards reset-map correctness. | TBD |
| B016  | D | STD MTS 016 force stop readback | 1 | Write CSR bit 1 high, then read word `0x0`; expect `force_stop=1` to persist until cleared. Verifies direct software control of the acceptance override. | Write CSR bit 1 high, then read word `0x0`; expect `force_stop=1` to persist until cleared. Verifies direct software control of the acceptance override. | TBD |
| B017  | D | STD MTS 017 soft reset self clear | 1 | Write CSR bit 2 high and poll back; expect the bit to pulse and then self-clear. Confirms the self-clearing implementation in `proc_avmm_csr`. | Write CSR bit 2 high and poll back; expect the bit to pulse and then self-clear. Confirms the self-clearing implementation in `proc_avmm_csr`. | TBD |
| B018  | D | STD MTS 018 bypass lapse readback | 1 | Write CSR bit 3 high and low; expect readback to match and subsequent datapath behavior to switch between padded and bypassed timestamping. Proves the mode latch. | Write CSR bit 3 high and low; expect readback to match and subsequent datapath behavior to switch between padded and bypassed timestamping. Proves the mode latch. | TBD |
| B019  | D | STD MTS 019 discard hiterr readback | 1 | Write CSR bit 4 high and low; expect readback to match and hit acceptance policy to track it. Proves the error-discard policy latch. | Write CSR bit 4 high and low; expect readback to match and hit acceptance policy to track it. Proves the error-discard policy latch. | TBD |
| B020  | D | STD MTS 020 op mode bits readback | 1 | Sweep `op_mode[30:28]`, then set/read/clear CSR bit 6. Verify bits 30 and 29 read back, bit 28 remains inert, bit 6 defaults low for physical arrival, and the overflow-base diagnostic selection round-trips exactly. | All implemented mode fields match masked readback; bit 6 returns to the production-default direct-arrival mode after clear. | `do_std_020_op_mode_bits_readback` |

## 4. CSR Semantics And Counter Behavior

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| B021  | D | STD MTS 021 expected latency zero write | 1 | Write `0` to CSR word `0x8`; expect readback `0` and a correspondingly strict error window for subsequent hits. Covers the lower bound. | Write `0` to CSR word `0x8`; expect readback `0` and a correspondingly strict error window for subsequent hits. Covers the lower bound. | TBD |
| B022  | D | STD MTS 022 expected latency small write | 1 | Write a small nonzero value to CSR word `0x8`; expect readback to match and the error threshold to shift accordingly. Proves mid-run configurability. | Write a small nonzero value to CSR word `0x8`; expect readback to match and the error threshold to shift accordingly. Proves mid-run configurability. | TBD |
| B023  | D | STD MTS 023 expected latency maxword write | 1 | Write `0xFFFFFFFF` to CSR word `0x8`; expect exact readback and a reference-model update of `expected_latency_1n6` and `padding_upper`. Covers the upper software range. | Write `0xFFFFFFFF` to CSR word `0x8`; expect exact readback and a reference-model update of `expected_latency_1n6` and `padding_upper`. Covers the upper software range. | TBD |
| B024  | D | STD MTS 024 unsupported write addr1 inert | 1 | Attempt a write to CSR word `0x4`; expect no hidden state change because address 1 has no write side effects. Locks down the sparse CSR map. | Attempt a write to CSR word `0x4`; expect no hidden state change because address 1 has no write side effects. Locks down the sparse CSR map. | TBD |
| B025  | D | STD MTS 025 unsupported write addr3 inert | 1 | Attempt a write to CSR word `0xC`; expect no change to counters or control fields. Guards against accidental write aliases. | Attempt a write to CSR word `0xC`; expect no change to counters or control fields. Guards against accidental write aliases. | TBD |
| B026  | D | STD MTS 026 unsupported write addr4 inert | 1 | Attempt a write to CSR word `0x10`; expect the lower total-count register to remain read-only. Guards against accidental write aliases. | Attempt a write to CSR word `0x10`; expect the lower total-count register to remain read-only. Guards against accidental write aliases. | TBD |
| B027  | D | STD MTS 027 unsupported read addr5 zero | 1 | Read an unsupported address such as word `0x14`; expect `readdata=0` with a clean acknowledgement. Defines the out-of-range software contract. | Read an unsupported address such as word `0x14`; expect `readdata=0` with a clean acknowledgement. Defines the out-of-range software contract. | TBD |
| B028  | D | STD MTS 028 csr waitrequest ack | 1 | Issue ordinary reads and writes and confirm `waitrequest` drops only for accepted accesses. Verifies the simple AVMM handshake used by the current RTL. | Issue ordinary reads and writes and confirm `waitrequest` drops only for accepted accesses. Verifies the simple AVMM handshake used by the current RTL. | TBD |
| B029  | D | STD MTS 029 csr burst of serial accesses | 1 | Execute a tight sequence of read and write transactions; expect no field corruption and deterministic final state. Exercises the lightweight CSR agent. | Execute a tight sequence of read and write transactions; expect no field corruption and deterministic final state. Exercises the lightweight CSR agent. | TBD |
| B030  | D | STD MTS 030 total counter counts all valid | 1 | Drive accepted and rejected valid beats; expect the total-hit counter to increment on every valid input beat as documented in `proc_discard_hit_cnt`. Confirms the intended accounting policy. | Drive accepted and rejected valid beats; expect the total-hit counter to increment on every valid input beat as documented in `proc_discard_hit_cnt`. Confirms the intended accounting policy. | TBD |

## 5. Input Acceptance And Discard Paths

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| B031  | D | STD MTS 031 running accepts clean hit | 1 | In `RUNNING` with `force_stop=0` and `discard_hiterr=1`, drive a clean valid beat; expect it to be accepted and eventually emitted as `hit_type1`. Establishes the base datapath contract. | In `RUNNING` with `force_stop=0` and `discard_hiterr=1`, drive a clean valid beat; expect it to be accepted and eventually emitted as `hit_type1`. Establishes the base datapath contract. | TBD |
| B032  | D | STD MTS 032 idle rejects clean hit | 1 | In `IDLE`, drive a valid beat while `ready=0`; expect no accepted hit, no `hit_type1` output, and no counter change because the ready/valid transfer did not occur. Proves that `IDLE` is not an acceptance state. | In `IDLE`, drive a valid beat while `ready=0`; expect no accepted hit, no `hit_type1` output, and no counter change because the ready/valid transfer did not occur. Proves that `IDLE` is not an acceptance state. | TBD |
| B033  | D | STD MTS 033 reset sclr flush accept | 1 | In `RESET/SCLR`, drive a valid beat; expect `asi_hit_type0_ready=1` and the beat to be accepted for flush behavior because `processor_allow_input=1` in this substate. Covers the flush-only ingress path. | In `RESET/SCLR`, drive a valid beat; expect `asi_hit_type0_ready=1` and the beat to be accepted for flush behavior because `processor_allow_input=1` in this substate. Covers the flush-only ingress path. | TBD |
| B034  | D | STD MTS 034 reset sync blocks hit | 1 | In `RESET/SYNC`, drive a valid beat; expect no acceptance because ready is low and `processor_allow_input=0`. Proves the synchronizing hold phase. | In `RESET/SYNC`, drive a valid beat; expect no acceptance because ready is low and `processor_allow_input=0`. Proves the synchronizing hold phase. | TBD |
| B035  | D | STD MTS 035 flushing accepts hit | 1 | In `FLUSHING`, drive a valid beat; expect acceptance and output under the current RTL contract. Documents the present post-terminate drain semantics. | In `FLUSHING`, drive a valid beat; expect acceptance and output under the current RTL contract. Documents the present post-terminate drain semantics. | TBD |
| B036  | D | STD MTS 036 hiterr discard enabled | 1 | In `RUNNING` with `discard_hiterr=1`, drive `error(HITERR_BIT_LOC)=1`; expect discard counting and no output beat. Verifies the default error policy. | In `RUNNING` with `discard_hiterr=1`, drive `error(HITERR_BIT_LOC)=1`; expect discard counting and no output beat. Verifies the default error policy. | TBD |
| B037  | D | STD MTS 037 hiterr discard disabled | 1 | In `RUNNING` with `discard_hiterr=0`, drive the same errored beat; expect it to be accepted and processed. Verifies policy override behavior. | In `RUNNING` with `discard_hiterr=0`, drive the same errored beat; expect it to be accepted and processed. Verifies policy override behavior. | TBD |
| B038  | D | STD MTS 038 force stop blocks acceptance | 1 | Assert `force_stop=1` in `RUNNING` and drive clean valid beats; expect `asi_hit_type0_ready` to stay high yet `hit_in_ok` to go low, causing rejection. Captures the current acceptance-vs-ready split. | Assert `force_stop=1` in `RUNNING` and drive clean valid beats; expect `asi_hit_type0_ready` to stay high yet `hit_in_ok` to go low, causing rejection. Captures the current acceptance-vs-ready split. | TBD |
| B039  | D | STD MTS 039 rejected hiterr still counts total | 1 | Drive a rejected hiterr beat and confirm the total counter increments while the discard counter increments too. Proves counter partitioning. | Drive a rejected hiterr beat and confirm the total counter increments while the discard counter increments too. Proves counter partitioning. | TBD |
| B040  | D | STD MTS 040 matched sideband and data fields | 1 | Drive a beat with matched sideband ASIC and payload channel fields; expect output `asic` and `channel` to follow the payload fields while packet bookkeeping follows the sideband channel. Documents the two-channel domains explicitly. | Drive a beat with matched sideband ASIC and payload channel fields; expect output `asic` and `channel` to follow the payload fields while packet bookkeeping follows the sideband channel. Documents the two-channel domains explicitly. | TBD |

## 6. Run-Control Sequence Baselines

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| B041  | D | STD MTS 041 legacy running plus one hit | 1 | Recreate the direct-to-`RUNNING` startup used by the legacy smoke TB and send one hit; expect one processed output. Preserves existing smoke semantics. | Recreate the direct-to-`RUNNING` startup used by the legacy smoke TB and send one hit; expect one processed output. Preserves existing smoke semantics. | TBD |
| B042  | D | STD MTS 042 standard prepare sync run | 1 | Execute the full `RUN_PREPARE -> SYNC -> RUNNING` sequence and send one hit; expect the same functional output as the legacy path. Establishes the standard-sequence baseline. | Execute the full `RUN_PREPARE -> SYNC -> RUNNING` sequence and send one hit; expect the same functional output as the legacy path. Establishes the standard-sequence baseline. | TBD |
| B043  | D | STD MTS 043 run prepare without sync | 1 | Send `RUN_PREPARE` and hold there; expect `RESET/SCLR` behavior with no normal RUNNING output. Documents partial-sequence behavior. | Send `RUN_PREPARE` and hold there; expect `RESET/SCLR` behavior with no normal RUNNING output. Documents partial-sequence behavior. | TBD |
| B044  | D | STD MTS 044 repeated sync pulses | 1 | Send repeated `SYNC` commands before `RUNNING`; expect the DUT to remain in the reset-sync hold state without emitting output. Covers control chatter in the arming phase. | Send repeated `SYNC` commands before `RUNNING`; expect the DUT to remain in the reset-sync hold state without emitting output. Covers control chatter in the arming phase. | TBD |
| B045  | D | STD MTS 045 terminating without eop then idle | 1 | Enter `FLUSHING` with no accepted payload EOP, assert upstream `endofrun`, and then send `IDLE` only after control ready returns; expect the empty close-marker boundary and no payload beats. Locks down the delivered terminal-boundary upgrade. | Enter `FLUSHING` with no accepted payload EOP, assert upstream `endofrun`, and then send `IDLE` only after control ready returns; expect the empty close-marker boundary and no payload beats. Locks down the delivered terminal-boundary upgrade. | TBD |
| B046  | D | STD MTS 046 running abort no flush | 1 | From `RUNNING`, send `IDLE` without `TERMINATING`; expect immediate state drop and no further acceptance. Covers the explicit abort branch. | From `RUNNING`, send `IDLE` without `TERMINATING`; expect immediate state drop and no further acceptance. Covers the explicit abort branch. | TBD |
| B047  | D | STD MTS 047 link test word is nonfunctional today | 1 | Drive the `LINK_TEST` control word; expect `run_state_cmd` to decode but no dedicated processor-state behavior. Documents current no-op semantics. | Drive the `LINK_TEST` control word; expect `run_state_cmd` to decode but no dedicated processor-state behavior. Documents current no-op semantics. | TBD |
| B048  | D | STD MTS 048 sync test word is nonfunctional today | 1 | Drive the `SYNC_TEST` control word; expect no dedicated processor-state behavior beyond the decoded command value. Documents current no-op semantics. | Drive the `SYNC_TEST` control word; expect no dedicated processor-state behavior beyond the decoded command value. Documents current no-op semantics. | TBD |
| B049  | D | STD MTS 049 reset word is nonfunctional today | 1 | Drive the `RESET` control word from `IDLE`; expect no dedicated processor-state branch. Documents the decoded-but-unhandled command. | Drive the `RESET` control word from `IDLE`; expect no dedicated processor-state branch. Documents the decoded-but-unhandled command. | TBD |
| B050  | D | STD MTS 050 out of daq word is nonfunctional today | 1 | Drive the `OUT_OF_DAQ` control word; expect no datapath-specific action beyond the decoded command. Documents the current implementation boundary. | Drive the `OUT_OF_DAQ` control word; expect no datapath-specific action beyond the decoded command. Documents the current implementation boundary. | TBD |

## 7. Timestamp Decode And Padding Basics

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| B051  | D | STD MTS 051 tcc uses rom decode | 1 | Drive a hit with a known raw `TCC`; expect the scoreboard to match the DUT’s `dual_port_rom` lookup before division. Grounds the decode model in the actual ROM. | Drive a hit with a known raw `TCC`; expect the scoreboard to match the DUT’s `dual_port_rom` lookup before division. Grounds the decode model in the actual ROM. | TBD |
| B052  | D | STD MTS 052 ecc uses second rom port | 1 | Drive a hit with a known raw `ECC`; expect the second ROM port to be used concurrently and mirrored by the scoreboard. Confirms the dual-port decode behavior. | Drive a hit with a known raw `ECC`; expect the second ROM port to be used concurrently and mirrored by the scoreboard. Confirms the dual-port decode behavior. | TBD |
| B053  | D | STD MTS 053 bypass off uses white timestamp | 1 | With `bypass_lapse=0`, expect the divider numerators to come from the padded 50-bit white timestamp path. Covers the normal operating mode. | With `bypass_lapse=0`, expect the divider numerators to come from the padded 50-bit white timestamp path. Covers the normal operating mode. | TBD |
| B054  | D | STD MTS 054 bypass on uses gray timestamp | 1 | With `bypass_lapse=1`, expect the divider numerators to be loaded directly from decoded gray values. Covers the debug bypass mode. | With `bypass_lapse=1`, expect the divider numerators to be loaded directly from decoded gray values. Covers the debug bypass mode. | TBD |
| B055  | D | STD MTS 055 expected latency updates padding upper | 1 | Rewrite `expected_latency`; expect subsequent hits to use the new delay-error threshold while the overflow `padding_upper` remains controlled by `MUTRIG_OVERFLOW_LOOKBACK_8N`. Preserves the legacy case name while proving the current split between timestamp-error latency and wrap disambiguation. | Rewrite `expected_latency`; expect subsequent hits to use the new delay-error threshold while the overflow `padding_upper` remains controlled by `MUTRIG_OVERFLOW_LOOKBACK_8N`. Preserves the legacy case name while proving the current split between timestamp-error latency and wrap disambiguation. | TBD |
| B056  | D | STD MTS 056 no adjust below upper bound | 1 | Choose a decoded timestamp below `padding_upper`; expect no overflow subtraction in the white-timestamp model. Covers the normal non-wrap path. | Choose a decoded timestamp below `padding_upper`; expect no overflow subtraction in the white-timestamp model. Covers the normal non-wrap path. | TBD |
| B057  | D | STD MTS 057 t path adjust above upper bound | 1 | Choose a T-path hit above `padding_upper` while the overflow-adjust latch is active; expect one overflow subtraction. Covers the T correction path. | Choose a T-path hit above `padding_upper` while the overflow-adjust latch is active; expect one overflow subtraction. Covers the T correction path. | TBD |
| B058  | D | STD MTS 058 e path adjust above upper bound | 1 | Choose an E-path hit above `padding_upper` while the overflow-adjust latch is active; expect one overflow subtraction on the E branch. Covers the E correction path. | Choose an E-path hit above `padding_upper` while the overflow-adjust latch is active; expect one overflow subtraction on the E branch. Covers the E correction path. | TBD |
| B059  | D | STD MTS 059 divider quotient populates tcc8n | 1 | Drive a deterministic hit and confirm `hit_type1.tcc_8n` matches the quotient of the selected white timestamp divided by 5. Locks down the primary transform. | Drive a deterministic hit and confirm `hit_type1.tcc_8n` matches the quotient of the selected white timestamp divided by 5. Locks down the primary transform. | TBD |
| B060  | D | STD MTS 060 divider remainder populates tcc1n6 | 1 | For the same style of hit, confirm `hit_type1.tcc_1n6` matches the remainder in the legal range 0..4. Locks down the sub-tick field. | For the same style of hit, confirm `hit_type1.tcc_1n6` matches the remainder in the legal range 0..4. Locks down the sub-tick field. | TBD |

## 8. ET / Op-Mode Basics

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| B061  | D | STD MTS 061 short mode zeroes et | 1 | With `derive_tot=0`, drive an `EFlag=1` hit and expect `ET_1n6=0`. Proves that short-mode ignores ToT generation. | With `derive_tot=0`, drive an `EFlag=1` hit and expect `ET_1n6=0`. Proves that short-mode ignores ToT generation. | TBD |
| B062  | D | STD MTS 062 tot mode masks eflag0 | 1 | With `derive_tot=1` and `EFlag=0`, expect `ET_1n6=0` regardless of decoded E/T delta. Proves the explicit EFlag mask. | With `derive_tot=1` and `EFlag=0`, expect `ET_1n6=0` regardless of decoded E/T delta. Proves the explicit EFlag mask. | TBD |
| B063  | D | STD MTS 063 tot mode positive delta | 1 | With `derive_tot=1` and a decoded positive delta, expect a positive nonzero `ET_1n6`. Establishes the basic long-mode ToT path. | With `derive_tot=1` and a decoded positive delta, expect a positive nonzero `ET_1n6`. Establishes the basic long-mode ToT path. | TBD |
| B064  | D | STD MTS 064 tot mode negative delta reference | 1 | With `derive_tot=1` and a crafted decoded negative delta, expect the current reference-model clamp behavior defined by the RTL plus the checked-in smoke TB. Keeps the plan aligned to today’s implementation rather than intuition. | With `derive_tot=1` and a crafted decoded negative delta, expect the current reference-model clamp behavior defined by the RTL plus the checked-in smoke TB. Keeps the plan aligned to today’s implementation rather than intuition. | TBD |
| B065  | D | STD MTS 065 tot mode saturates above 511 | 1 | With `derive_tot=1` and a decoded delta above 511, expect `ET_1n6=511`. Confirms saturation behavior. | With `derive_tot=1` and a decoded delta above 511, expect `ET_1n6=511`. Confirms saturation behavior. | TBD |
| B066  | D | STD MTS 066 delay field t path | 1 | With `delay_ts_field_use_t=1`, expect `debug_ts`, `debug_burst`, and `aso_hit_type1_error` to use the T quotient path. Covers the default delay-field selection. | With `delay_ts_field_use_t=1`, expect `debug_ts`, `debug_burst`, and `aso_hit_type1_error` to use the T quotient path. Covers the default delay-field selection. | TBD |
| B067  | D | STD MTS 067 delay field e path | 1 | With `delay_ts_field_use_t=0`, expect those same observables to use the E quotient path. Covers the alternate path. | With `delay_ts_field_use_t=0`, expect those same observables to use the E quotient path. Covers the alternate path. | TBD |
| B068  | D | STD MTS 068 tfine passthrough | 1 | Drive distinct `TFine` values and expect them to emerge unchanged in `hit_type1`. Guards the fine-time pipeline. | Drive distinct `TFine` values and expect them to emerge unchanged in `hit_type1`. Guards the fine-time pipeline. | TBD |
| B069  | D | STD MTS 069 asic passthrough | 1 | Drive payload ASIC 9 while selecting local mux slot 1 in AVST channel bits `[5:4]`. | Type1 ASIC is source slot 1, not the stale payload ASIC field; channel and timestamp math remain unchanged. | `do_std_069_asic_passthrough` |
| B070  | D | STD MTS 070 channel passthrough | 1 | Drive distinct payload channel values and expect them to emerge unchanged in `aso_hit_type1_data[34:30]`. Guards channel identity propagation. | Drive distinct payload channel values and expect them to emerge unchanged in `aso_hit_type1_data[34:30]`. Guards channel identity propagation. | TBD |

## 9. Output Marker Basics

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| B071  | D | STD MTS 071 sop first hit channel0 | 1 | In a default enabled-window build, drive the first accepted hit for channel 0 in a run; expect `startofpacket=1` on that output beat. Proves per-channel start-of-run marking. | In a default enabled-window build, drive the first accepted hit for channel 0 in a run; expect `startofpacket=1` on that output beat. Proves per-channel start-of-run marking. | TBD |
| B072  | D | STD MTS 072 sop first hit channel1 | 1 | Repeat for channel 1; expect one SOP. Covers the second enabled channel. | Repeat for channel 1; expect one SOP. Covers the second enabled channel. | TBD |
| B073  | D | STD MTS 073 sop first hit channel2 | 1 | Repeat for channel 2; expect one SOP. Covers the third enabled channel. | Repeat for channel 2; expect one SOP. Covers the third enabled channel. | TBD |
| B074  | D | STD MTS 074 sop first hit channel3 | 1 | Repeat for channel 3; expect one SOP. Covers the fourth enabled channel. | Repeat for channel 3; expect one SOP. Covers the fourth enabled channel. | TBD |
| B075  | D | STD MTS 075 no repeated sop same channel | 1 | Drive a second accepted hit for a previously seen enabled channel in the same run; expect no repeated SOP. Verifies `startofrun_sent` persistence. | Drive a second accepted hit for a previously seen enabled channel in the same run; expect no repeated SOP. Verifies `startofrun_sent` persistence. | TBD |
| B076  | D | STD MTS 076 reset clears startofrun sent | 1 | Enter `RESET` and start a new run; expect SOP eligibility to return for each enabled channel. Verifies reset cleanup. | Enter `RESET` and start a new run; expect SOP eligibility to return for each enabled channel. Verifies reset cleanup. | TBD |
| B077  | D | STD MTS 077 terminating input eop forwards output eop | 1 | In `TERMINATING`, accept an input beat with `endofpacket=1` and the explicit upstream `endofrun` pulse; expect the lane-close output `endofpacket` train after payload drain. Proves the current termination-EOP pipeline. | In `TERMINATING`, accept an input beat with `endofpacket=1` and the explicit upstream `endofrun` pulse; expect the lane-close output `endofpacket` train after payload drain. Proves the current termination-EOP pipeline. | TBD |
| B078  | D | STD MTS 078 nonterminating eop not forwarded | 1 | Accept an input beat with `endofpacket=1` while still in `RUNNING`; expect no output EOP. Documents the current “termination-only” EOP forwarding. | Accept an input beat with `endofpacket=1` while still in `RUNNING`; expect no output EOP. Documents the current “termination-only” EOP forwarding. | TBD |
| B079  | D | STD MTS 079 empty stays zero | 1 | Observe normal hit traffic and confirm `aso_hit_type1_empty` remains `0`. Locks down the current constant-zero contract. | Observe normal hit traffic and confirm `aso_hit_type1_empty` remains `0`. Locks down the current constant-zero contract. | TBD |
| B080  | D | STD MTS 080 output valid only in run or flush | 1 | Drive accepted hits across `IDLE`, `RESET`, `RUNNING`, and `FLUSHING`; expect `aso_hit_type1_valid` only in `RUNNING` or `FLUSHING` when `hit_out.valid=1`. Verifies the source gating. | Drive accepted hits across `IDLE`, `RESET`, `RUNNING`, and `FLUSHING`; expect `aso_hit_type1_valid` only in `RUNNING` or `FLUSHING` when `hit_out.valid=1`. Verifies the source gating. | TBD |

## 10. Output Routing And Error Basics

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| B081  | D | STD MTS 081 route lane0 | 1 | Craft a quotient whose bits `[5:4]` are `00`; expect output route channel `0000`. Verifies lane-0 routing. | Craft a quotient whose bits `[5:4]` are `00`; expect output route channel `0000`. Verifies lane-0 routing. | TBD |
| B082  | D | STD MTS 082 route lane1 | 1 | Craft a quotient whose bits `[5:4]` are `01`; expect output route channel `0001`. Verifies lane-1 routing. | Craft a quotient whose bits `[5:4]` are `01`; expect output route channel `0001`. Verifies lane-1 routing. | TBD |
| B083  | D | STD MTS 083 route lane2 | 1 | Craft a quotient whose bits `[5:4]` are `10`; expect output route channel `0010`. Verifies lane-2 routing. | Craft a quotient whose bits `[5:4]` are `10`; expect output route channel `0010`. Verifies lane-2 routing. | TBD |
| B084  | D | STD MTS 084 route lane3 | 1 | Craft a quotient whose bits `[5:4]` are `11`; expect output route channel `0011`. Verifies lane-3 routing. | Craft a quotient whose bits `[5:4]` are `11`; expect output route channel `0011`. Verifies lane-3 routing. | TBD |
| B085  | D | STD MTS 085 error low in range | 1 | Drive a hit whose `debug_ts` is positive and strictly below `expected_latency`; expect `aso_hit_type1_error=0`. Proves the clean window. | Drive a hit whose `debug_ts` is positive and strictly below `expected_latency`; expect `aso_hit_type1_error=0`. Proves the clean window. | TBD |
| B086  | D | STD MTS 086 error high at zero | 1 | Drive a hit whose selected timestamp yields `debug_ts=0`; expect `aso_hit_type1_error=1`. Covers the lower edge of the strict inequality. | Drive a hit whose selected timestamp yields `debug_ts=0`; expect `aso_hit_type1_error=1`. Covers the lower edge of the strict inequality. | TBD |
| B087  | D | STD MTS 087 error high for negative | 1 | Drive a hit whose selected timestamp appears ahead of the GTS counter; expect `aso_hit_type1_error=1`. Covers the negative window. | Drive a hit whose selected timestamp appears ahead of the GTS counter; expect `aso_hit_type1_error=1`. Covers the negative window. | TBD |
| B088  | D | STD MTS 088 error high at or above limit | 1 | Drive a hit with `debug_ts >= expected_latency`; expect `aso_hit_type1_error=1`. Covers the upper edge. | Drive a hit with `debug_ts >= expected_latency`; expect `aso_hit_type1_error=1`. Covers the upper edge. | TBD |
| B089  | D | STD MTS 089 debug ts valid alignment | 1 | Confirm `aso_debug_ts_valid` aligns with the processed-hit pipeline stage rather than the raw input beat. Locks down debug timing. | Confirm `aso_debug_ts_valid` aligns with the processed-hit pipeline stage rather than the raw input beat. Locks down debug timing. | TBD |
| B090  | D | STD MTS 090 delay field changes error source | 1 | Toggle `delay_ts_field_use_t` between runs and confirm the error decision follows the selected timestamp source. Verifies the selected-path dependency. | Toggle `delay_ts_field_use_t` between runs and confirm the error decision follows the selected timestamp source. Verifies the selected-path dependency. | TBD |

## 11. Debug-Burst And Delta Basics

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| B091  | D | STD MTS 091 debug burst only running | 1 | Observe the DUT in `IDLE`, `RESET`, and `RUNNING`; expect `aso_debug_burst_valid` only in `RUNNING`. Locks down the state gate. | Observe the DUT in `IDLE`, `RESET`, and `RUNNING`; expect `aso_debug_burst_valid` only in `RUNNING`. Locks down the state gate. | TBD |
| B092  | D | STD MTS 092 ts delta only running | 1 | Make the same observation for `aso_ts_delta_valid`; expect activity only in `RUNNING`. Locks down the state gate. | Make the same observation for `aso_ts_delta_valid`; expect activity only in `RUNNING`. Locks down the state gate. | TBD |
| B093  | D | STD MTS 093 first running hit warms history | 1 | Send the first processed hit after entering `RUNNING`; expect history seeding without stale delta leakage. Proves clean startup for the delta logic. | Send the first processed hit after entering `RUNNING`; expect history seeding without stale delta leakage. Proves clean startup for the delta logic. | TBD |
| B094  | D | STD MTS 094 positive timestamp delta | 1 | Send two accepted hits with increasing selected timestamps; expect a positive sign in `debug_burst` and positive `ts_delta`. Proves the positive branch. | Send two accepted hits with increasing selected timestamps; expect a positive sign in `debug_burst` and positive `ts_delta`. Proves the positive branch. | TBD |
| B095  | D | STD MTS 095 negative timestamp delta | 1 | Send two accepted hits with decreasing selected timestamps; expect a negative sign and negative `ts_delta`. Proves the negative branch. | Send two accepted hits with decreasing selected timestamps; expect a negative sign and negative `ts_delta`. Proves the negative branch. | TBD |
| B096  | D | STD MTS 096 zero timestamp delta | 1 | Send two accepted hits with equal selected timestamps; expect a zero-like delta presentation. Covers the no-change branch. | Send two accepted hits with equal selected timestamps; expect a zero-like delta presentation. Covers the no-change branch. | TBD |
| B097  | D | STD MTS 097 positive signmag conversion | 1 | Check that positive sign-magnitude `delta_timestamp` is converted to positive two’s complement in `aso_ts_delta_data`. Locks down helper-function semantics. | Check that positive sign-magnitude `delta_timestamp` is converted to positive two’s complement in `aso_ts_delta_data`. Locks down helper-function semantics. | TBD |
| B098  | D | STD MTS 098 negative signmag conversion | 1 | Check the same for a negative sign-magnitude value. Locks down helper-function semantics. | Check the same for a negative sign-magnitude value. Locks down helper-function semantics. | TBD |
| B099  | D | STD MTS 099 arrival delta uses gts | 1 | Send two equal true timestamps at different GTS arrival times. Check the debug-burst inter-arrival delta and, on both Type1 beats, the full-width identity `latency48 = arrival48 - true_hit_ts48`. | Timestamp delta remains zero, arrival delta advances, both identity checks pass, and production lifetime is nonnegative. | `do_std_099_arrival_delta_uses_gts` |
| B100  | D | STD MTS 100 debug streams clear outside running | 1 | Leave `RUNNING` and confirm debug-valid signals and stored outputs clear promptly. Guards stale history after stop. | Leave `RUNNING` and confirm debug-valid signals and stored outputs clear promptly. Guards stale history after stop. | TBD |

## 12. Legacy Smoke And Counter Checks

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| B101  | D | STD MTS 101 replay smoke positive et | 1 | Reproduce the checked-in smoke vector that expects a positive `ET_1n6` result after writing `0x40000001`. Keeps the new harness anchored to the existing bench. | Reproduce the checked-in smoke vector that expects a positive `ET_1n6` result after writing `0x40000001`. Keeps the new harness anchored to the existing bench. | TBD |
| B102  | D | STD MTS 102 replay smoke eflag zero | 1 | Reproduce the smoke vector with `EFlag=0` and expect `ET_1n6=0`. Preserves the current directed check. | Reproduce the smoke vector with `EFlag=0` and expect `ET_1n6=0`. Preserves the current directed check. | TBD |
| B103  | D | STD MTS 103 replay smoke clamp vector | 1 | Reproduce the smoke vector that expects the clamp/saturation path. Preserves the current directed check. | Reproduce the smoke vector that expects the clamp/saturation path. Preserves the current directed check. | TBD |
| B104  | D | STD MTS 104 discard counter matches rejections | 1 | Drive a known mix of rejected hits and confirm the CSR discard counter matches exactly. Validates software observability of discard policy. | Drive a known mix of rejected hits and confirm the CSR discard counter matches exactly. Validates software observability of discard policy. | TBD |
| B105  | D | STD MTS 105 total counter matches all valid | 1 | Drive a known mix of accepted and rejected hits and confirm the total counter matches all valid inputs. Validates software observability of ingress load. | Drive a known mix of accepted and rejected hits and confirm the total counter matches all valid inputs. Validates software observability of ingress load. | TBD |
| B106  | D | STD MTS 106 total counter hi rollover | 1 | Enable the DV-only counter seed generic, place the total counter at `0x0000_FFFF_FFFF`, accept one clean hit, and confirm the high word increments to `0x0001`. Covers the 48-bit accounting carry path without a 2^32-cycle simulation. | Enable the DV-only counter seed generic, place the total counter at `0x0000_FFFF_FFFF`, accept one clean hit, and confirm the high word increments to `0x0001`. Covers the 48-bit accounting carry path without a 2^32-cycle simulation. | TBD |
| B107  | D | STD MTS 107 soft reset clears counters | 1 | Pulse `soft_reset` after traffic and confirm both counters clear without requiring global reset. Verifies the intended software reset path. | Pulse `soft_reset` after traffic and confirm both counters clear without requiring global reset. Verifies the intended software reset path. | TBD |
| B108  | D | STD MTS 108 sync clears counters | 1 | Use the standard `RUN_PREPARE -> SYNC` path after traffic and confirm the counters clear through `reset_flow=SYNC`. Verifies the run-sequence reset path. | Use the standard `RUN_PREPARE -> SYNC` path after traffic and confirm the counters clear through `reset_flow=SYNC`. Verifies the run-sequence reset path. | TBD |
| B109  | D | STD MTS 109 running status bit semantics | 1 | Read CSR word `0x0` in `RUNNING` and outside `RUNNING`; expect bit 0 to mirror `processor_state==RUNNING` exactly. Documents the live-status semantics. | Read CSR word `0x0` in `RUNNING` and outside `RUNNING`; expect bit 0 to mirror `processor_state==RUNNING` exactly. Documents the live-status semantics. | TBD |
| B110  | D | STD MTS 110 force stop persists until cleared | 1 | Set `force_stop`, verify blocked acceptance, then clear it and confirm acceptance resumes. Proves stable software override behavior. | Set `force_stop`, verify blocked acceptance, then clear it and confirm acceptance resumes. Proves stable software override behavior. | TBD |

## 13. Parameter And Generic Baselines

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| B111  | D | STD MTS 111 compile rtl default div pipeline | 1 | Build and run with the RTL default `LPM_DIV_PIPELINE=4`; expect the reference latency and the correct math. Establishes the source-file baseline. | Build and run with the RTL default `LPM_DIV_PIPELINE=4`; expect the reference latency and the correct math. Establishes the source-file baseline. | TBD |
| B112  | D | STD MTS 112 compile packaged div pipeline | 1 | Build and run with the package-visible `LPM_DIV_PIPELINE=2` override; expect the same math with the shorter measured pipeline delay. The current packaged default is `4`, so this is a compatibility override rather than the default. | Build and run with the package-visible `LPM_DIV_PIPELINE=2` override; expect the same math with the shorter measured pipeline delay. The current packaged default is `4`, so this is a compatibility override rather than the default. | TBD |
| B113  | D | STD MTS 113 single enabled channel window | 1 | Build with `ENABLED_CHANNEL_LO=0` and `ENABLED_CHANNEL_HI=0`; expect packet-open bookkeeping to track sideband channel 0 and ignore an outside-window sideband lane. Covers the smallest enabled window. | Build with `ENABLED_CHANNEL_LO=0` and `ENABLED_CHANNEL_HI=0`; expect packet-open bookkeeping to track sideband channel 0 and ignore an outside-window sideband lane. Covers the smallest enabled window. | TBD |
| B114  | D | STD MTS 114 upper enabled window | 1 | Build with `ENABLED_CHANNEL_LO=2` and `ENABLED_CHANNEL_HI=3`; expect packet-open bookkeeping to track channels 2 and 3 and ignore a lower outside-window lane. Covers an offset window. | Build with `ENABLED_CHANNEL_LO=2` and `ENABLED_CHANNEL_HI=3`; expect packet-open bookkeeping to track channels 2 and 3 and ignore a lower outside-window lane. Covers an offset window. | TBD |
| B115  | D | STD MTS 115 remapped hiterr bit | 1 | Build with a remapped `HITERR_BIT_LOC` and drive the matching error bit; expect discard behavior to follow the remap. Proves that only the configured bit matters. | Build with a remapped `HITERR_BIT_LOC` and drive the matching error bit; expect discard behavior to follow the remap. Proves that only the configured bit matters. | TBD |
| B116  | D | STD MTS 116 remapped crcerr still inert | 1 | Build with a remapped `CRCERR_BIT_LOC`; expect no functional change because the current RTL does not consume that bit. Documents a deliberate no-effect parameter. | Build with a remapped `CRCERR_BIT_LOC`; expect no functional change because the current RTL does not consume that bit. Documents a deliberate no-effect parameter. | TBD |
| B117  | D | STD MTS 117 remapped frame corrupt still inert | 1 | Build with a remapped `FRAME_CORRPT_BIT_LOC`; expect no functional change. Documents another deliberate no-effect parameter. | Build with a remapped `FRAME_CORRPT_BIT_LOC`; expect no functional change. Documents another deliberate no-effect parameter. | TBD |
| B118  | D | STD MTS 118 changed latency generic at power on | 1 | Build with a nondefault `MUTRIG_BUFFER_EXPECTED_LATENCY_8N`; expect the power-on CSR default to follow that generic. Covers generic-to-CSR propagation. | Build with a nondefault `MUTRIG_BUFFER_EXPECTED_LATENCY_8N`; expect the power-on CSR default to follow that generic. Covers generic-to-CSR propagation. | TBD |
| B119  | D | STD MTS 119 bank string is debug only | 1 | Build with `BANK="DOWN"` and compare against `BANK="UP"`; expect only debug report text to differ. Documents the nonfunctional generic. | Build with `BANK="DOWN"` and compare against `BANK="UP"`; expect only debug report text to differ. Documents the nonfunctional generic. | TBD |
| B120  | D | STD MTS 120 debug zero is functionally equivalent | 1 | Build with `DEBUG=0`; expect report suppression but identical functional outputs and counters. Guards against accidental debug dependency. | Build with `DEBUG=0`; expect report suppression but identical functional outputs and counters. Guards against accidental debug dependency. | TBD |

## 14. Termination And Upgrade-Gating Basics

| ID | Method | Scenario | Iter | Stimulus | Pass Criteria | Function Reference |
|----|--------|----------|------|----------|---------------|-------------------|
| B121  | D | STD MTS 121 preterminate hit still drains | 1 | Accept a hit immediately before `TERMINATING`; expect it to continue through the pipeline and emerge during `FLUSHING`. Proves current drain behavior. | Accept a hit immediately before `TERMINATING`; expect it to continue through the pipeline and emerge during `FLUSHING`. Proves current drain behavior. | TBD |
| B122  | D | STD MTS 122 terminating eop and hit emit final boundary | 1 | In `TERMINATING`, accept a hit carrying `endofpacket=1` plus the upstream `endofrun`; expect payload drain followed by the empty close-marker train. Proves the happy-path stop contract currently implemented. | In `TERMINATING`, accept a hit carrying `endofpacket=1` plus the upstream `endofrun`; expect payload drain followed by the empty close-marker train. Proves the happy-path stop contract currently implemented. | TBD |
| B123  | D | STD MTS 123 flushing accepts more hits today | 1 | Continue driving clean hits in `FLUSHING`; expect them to be accepted under the current RTL. Documents the present post-stop openness. | Continue driving clean hits in `FLUSHING`; expect them to be accepted under the current RTL. Documents the present post-stop openness. | TBD |
| B124  | D | STD MTS 124 flushing quiet without hits | 1 | Enter `FLUSHING` and stop all input traffic; expect no spontaneous output. Proves that flush does not invent traffic on its own. | Enter `FLUSHING` and stop all input traffic; expect no spontaneous output. Proves that flush does not invent traffic on its own. | TBD |
| B125  | D | STD MTS 125 ctrl ready high through terminate | 1 | Historical case name retained; current RTL must deassert `asi_ctrl_ready` during `RUNNING -> TERMINATING` and restore it only after the close-marker train. Documents the delivered stateful handshake. | Historical case name retained; current RTL must deassert `asi_ctrl_ready` during `RUNNING -> TERMINATING` and restore it only after the close-marker train. Documents the delivered stateful handshake. | TBD |
| B126  | D | STD MTS 126 ctrl ready high through prepare and sync | 1 | Historical case name retained; current RTL must deassert `asi_ctrl_ready` during the `RUN_PREPARE` and `SYNC` local reset phases, then restore it at each phase completion. | Historical case name retained; current RTL must deassert `asi_ctrl_ready` during the `RUN_PREPARE` and `SYNC` local reset phases, then restore it at each phase completion. | TBD |
| B127  | D | STD MTS 127 upgrade case stateful ready on terminate | 1 | Prove the delivered upgrade behavior in which `asi_ctrl_ready` remains low until upstream `endofrun`, local drain, and close-marker generation finish, then accept `RUN_PREPARE -> SYNC -> RUNNING` as a clean re-arm sequence from completed `FLUSHING`. | Prove the delivered upgrade behavior in which `asi_ctrl_ready` remains low until upstream `endofrun`, local drain, and close-marker generation finish, then accept `RUN_PREPARE -> SYNC -> RUNNING` as a clean re-arm sequence from completed `FLUSHING`. | TBD |
| B128  | D | STD MTS 128 upgrade case terminal boundary without extra hits | 1 | Prove the delivered upgrade behavior in which the terminate boundary remains observable even when no fresh post-edge hits are accepted. | Prove the delivered upgrade behavior in which the terminate boundary remains observable even when no fresh post-edge hits are accepted. | TBD |
| B129  | D | STD MTS 129 upgrade case idle after boundary only | 1 | Prove the delivered upgrade behavior in which a well-behaved `IDLE` command is not acknowledged until the terminal boundary and local drain are complete. | Prove the delivered upgrade behavior in which a well-behaved `IDLE` command is not acknowledged until the terminal boundary and local drain are complete. | TBD |
| B130  | D | STD MTS 130 full standard sequence baseline | 1 | Use `RUN_PREPARE -> SYNC -> RUNNING -> TERMINATING -> IDLE` with one packet per enabled channel as the canonical baseline scenario for the later UVM harness. This is the primary signoff narrative for the IP. | Use `RUN_PREPARE -> SYNC -> RUNNING -> TERMINATING -> IDLE` with one packet per enabled channel as the canonical baseline scenario for the later UVM harness. This is the primary signoff narrative for the IP. | TBD |
